services:
  - type: web
    name: mathare-igad
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --chdir mathare mathare.wsgi:application --bind 0.0.0.0:$PORT"
